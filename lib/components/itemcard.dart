import 'package:flutter/material.dart';

class ItemCard extends StatelessWidget {
  final double value;
  final int quantity;
  final DateTime date;

  ItemCard(this.date, this.quantity, this.value);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
